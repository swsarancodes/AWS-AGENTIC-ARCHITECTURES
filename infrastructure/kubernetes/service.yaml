apiVersion: v1
kind: Service
metadata:
  name: agentic-api
  namespace: agentic-architectures
spec:
  selector:
    app: agentic-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
